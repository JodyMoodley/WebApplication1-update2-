@model IEnumerable<WebApplication1.Models.CheckIn>

@{
    ViewBag.Title = "Index";
}

<h2>Checkin</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoomReservations.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheckInDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoomReservations.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheckInDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CheckInID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CheckInID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CheckInID })
        </td>
    </tr>
}

</table>
